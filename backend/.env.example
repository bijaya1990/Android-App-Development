APP_ENV=production
APP_URL=https://example.com

DB_HOST=localhost
DB_NAME=talpadar_news
DB_USER=change_me
DB_PASS=change_me

# Set to 1 once the site is served over HTTPS (required on production).
SESSION_COOKIE_SECURE=1
